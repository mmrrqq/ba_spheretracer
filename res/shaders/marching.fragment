#version 410 core

vec3 eyePosition = vec3(0.0, 0.0, 3.0);

in vec2 TexCoords;
out vec4 color;

void main()
{
    vec3 rayDirection = normalize(vec3(TexCoords, 0.0) - eyePosition);
    color = vec4(rayDirection, 1.0);
}